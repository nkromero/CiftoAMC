# Copy this file to .env and fill in your AMCSD credentials.
# .env is git-ignored, so your credentials never get committed.
# The page fetches this file and exchanges these for a token at load time;
# the token is then held in memory only, never written back to disk.
username=
password=
